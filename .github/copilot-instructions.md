# Copilot Instructions

## General Guidelines
- When running complex computations in the terminal, write a script file (Python/PowerShell) instead of using long inline commands. The terminal has character/complexity limits for inline commands.
