## What's New — v1.2.0

- Formula code now uses `BigInteger` instead of `long`, except in the `Gcd` method, where `BigInteger` is not needed.
- The proof document was polished, and several proofs were reorganized for better clarity.
